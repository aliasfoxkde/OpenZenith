[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "openzenith-core"
dynamic = ["version"]
requires-python = ">=3.10"

[tool.maturin]
manifest-path = "Cargo.toml"
features = ["pyo3/numpy"]
